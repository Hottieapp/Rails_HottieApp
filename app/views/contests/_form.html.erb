<%= form_for(@contest) do |f| %>
  <% if @contest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contest.errors.count, "error") %> prohibited this contest from being saved:</h2>

      <ul>
      <% @contest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Contest_Title %><br>
    <%= f.text_field :Contest_Title %>
  </div>
  <div class="field">
    <%= f.label :Contest_type %><br>
    <%= f.text_field :Contest_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
